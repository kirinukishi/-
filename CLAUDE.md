# CLAUDE.md

このリポジトリで Claude Code が作業するときのルール。

## リポジトリの性質

- `kirinukishi/-` はナレッジベース兼ブレスト用リポジトリ
- `ideas/` にアイデア文書、`tips/` に学習ノートが入る
- コードを実行するプロジェクトではない（現状）

## セキュリティルール（絶対）

### .env / 機密情報の扱い

- **`.env` ファイルを読み込まない・編集しない・内容を表示しない**
- APIキー・トークン・パスワード・接続文字列・秘密鍵が書かれたファイルも同様
- もし誤って開いた場合は即座に作業を停止してユーザーに報告する
- 機密情報を検知したら以下を禁止:
  - コミットへの含有
  - 別ファイルへのコピー
  - ログ/標準出力への表示
  - 外部通信での送信

### .gitignore 必須項目

新しいサブプロジェクトを作るときは必ず以下を `.gitignore` に入れる:

```
.env
.env.*
!.env.example
*.key
*.pem
secrets/
credentials.json
.claude/file-history/
```

### コミット前チェック

- `git log` / `git diff` でシークレットが混入していないか確認
- 特に以下のパターンに注意: `sk-`, `ghp_`, `AKIA`, `xox[bpars]-`, `-----BEGIN`

## 作業スタイル

### ドキュメントの書き方

- 日付プレフィックス付きファイル名: `YYYY-MM-DD_題名.md`
- `ideas/` = アイデア・企画・戦略
- `tips/` = 学習ノート・ハウツー・参考情報

### ブランチ運用

- 作業ブランチは `claude/<task-name>-<session-id>` 形式
- `main` は存在しない（orphan ブランチ運用）
- プッシュは必ず作業ブランチへ

### コミットメッセージ

- 日本語でOK
- 1行目は要約、本文で「なぜ」を説明
- 定形フッター（Claude Code リンク）は付けない

## 禁止事項

- `.env` の読込・編集（最重要）
- 実際のAPIキー・トークンのドキュメントへの記載（例として書くなら `sk-xxxxx` のようにマスク）
- サンドボックス外での破壊的コマンド（`rm -rf`, `git push --force` 等）の無許可実行
- ユーザーに確認せずに外部サービスへのアップロード
- 使い回しAPIキーの生成提案（サービスごとに別キー）

## 参考

セキュリティの詳細は `tips/2026-04-14_claude-code-security-complete.md` 参照。
